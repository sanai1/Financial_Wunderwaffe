<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Выбранное состояние -->
    <item android:color="@color/green_selected" android:state_checked="true" />
    <!-- Невыбранное состояние -->
    <item android:color="@color/green_unselected" android:state_checked="false" />
</selector>